# Vault Vintage — Backend
# Railway inyecta PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD automáticamente
# Solo necesitas configurar JWT_SECRET si quieres

PORT=8082
JWT_SECRET=clave_super_secreta_vault_vintage_2026_spring_boot
SPRING_JPA_SHOW_SQL=false

# --- Railway (producción) ---
# Railway PostgreSQL plugin inyecta estas variables automáticamente:
# PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD
# No necesitas configurarlas manualmente si usas el plugin de PostgreSQL
