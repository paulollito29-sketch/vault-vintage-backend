server.port=${PORT:8082}
spring.application.name=vault_vintage

spring.datasource.url=jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:vault_vintage}
spring.datasource.username=${PGUSER:vvintage}
spring.datasource.password=${PGPASSWORD:vvintage123}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect